name: shadow_xjy_manager_github_io
description: shAdow-XJY Manager GitHub Pages - Personal portfolio and project showcase.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2

  url_launcher: ^6.1.0
  flutter_markdown: ^0.6.10
  markdown: ^5.0.0
  assets_audio_player: ^3.0.4+1
  auto_size_text: ^3.0.0
  responsive_builder: ^0.4.2
  vertical_card_pager: ^1.5.0

  list_twolevel: ^0.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/fonts/WDXL_LICENSE.txt
    - assets/fonts/site/Roboto_LICENSE.txt
    - assets/image/
    - assets/image/video/
    - assets/image/favorite/
    - assets/image/collections/
    - assets/image/book/redesign/


    - assets/icon/

    - assets/music/

    - assets/video/


  fonts:
    - family: WDXL
      fonts:
        - asset: assets/fonts/WDXLLubrifontSC-Regular.ttf

    - family: SiteBody
      fonts:
        - asset: assets/fonts/site/Roboto-Regular.ttf
